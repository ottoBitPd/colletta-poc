/*import {ClassClient} from "../../src/ts/model/ClassClient";
import {expect} from 'chai';
import 'mocha';


describe('#getMethods', function() {

let obj=new ClassClient();

    context('ClassClientTest.getDbClassManager()', function () {
        it('should return database class', function () {
            let one=new ClassClient();
            expect(obj).to.equal(one);
        });
    });






});*/